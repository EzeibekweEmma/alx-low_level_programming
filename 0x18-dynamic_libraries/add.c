#include "operations.h"

/**
 * add - function that add two numbers
 * @a: first operand
 * @b: second operand
 * Return: Answer
 */

int add(int a, int b)
{
	int ans;

	ans = a + b;
	return (ans);
}
