#include "operations.h"

/**
 * sub - function that subtract two numbers
 * @a: first operand
 * @b: second operand
 * Return: Answer
 */

int sub(int a, int b)
{
	int ans;

	ans = a - b;
	return (ans);
}
